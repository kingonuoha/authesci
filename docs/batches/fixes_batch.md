## Fixes Batch


system logs should be in the same page as notification for admin since it is a tab 
#2 add logs to everywhere necessary. number 
3 the first five messages should show but unread will be on top currently not shows. 
let us have a universal online states.  to check online status of everyone we can also have a dedicated profile components for it so. anywhere a user profile photo is, the online state of that user shows.
 There is no payroll for admins to payout users. 
 admin analytics and stats are not showing on dashboard. 
 Admin job and managed users should be in card form. 
 - Admin can make new admin from the users list with the button.
- Updates to the auth sign up page. Selecting a role shows a brief card below describing what the roles consist of.
- On registration, after the event is sent, I get redirected to `auth/verified-email` which returns a 404 error, negatively impacting UX.
Jobs should show no of applicants on the index page
when new job ia posted and paid for, emails should be sent to all scientists notifying them of the new listing
Employer dash, recent applications should be in card form, and there should be a view all  button, and for each applicant a chat icon to send them message should be present to
active projects shold have an invite button, the invite opens a modal where they can paste an email(dont show suggested email accounts) once they oaste it, the system sends them and email and notification with the link to accept the invitation, on accept the invitation the user is added to the project and the project is added to the user's active projects, and they join as collabortor, if the email is not found show error message, also show a card tjay saus yhe email user must have an account on  authesci
Active projects, recent activity log needs to be implemented
add pagination or lazy load for everywhere we ttend to have a large list of items, either pagination or infinite scroll
employer can add questions to a job whilst creating or editing, these questions are asked to the applicant when they apply for the job
add chat to sidebar for easy access
for both scientist and employer dashboard, add a carousel conaining categories, should have images to, and the images would be dynamiclly fetched from pixels using their api service, i have an api key already, we can utilize it, we can call it featured skills
update the dash footer to show the current year